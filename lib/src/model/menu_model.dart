class MenuModel {
  final String title, cover;
  final int itemCount;
  final bool isHasMenu;

  const MenuModel({required this.title, required this.cover, required this.itemCount, required this.isHasMenu});
}
